{
    "name": "anthropic-proxy",
    "version": "1.0.0",
    "description": "Cloudflare Worker to proxy requests to Anthropic API",
    "main": "worker.js",
    "scripts": {
        "deploy": "npx wrangler deploy",
        "dev": "npx wrangler dev",
        "secret": "npx wrangler secret put ANTHROPIC_API_KEY"
    },
    "dependencies": {
        "wrangler": "^3.114.0"
    }
}